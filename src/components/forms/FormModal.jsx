import { Dialog } from '@headlessui/react';
import { AnimatePresence, motion } from 'framer-motion';
import { useForm } from 'react-hook-form';
import { Button } from '../ui/Button.jsx';
import { Input } from '../ui/Input.jsx';
export function FormModal({ open, onClose, title, columns, initialData, onSubmit }){ const {register,handleSubmit,reset,formState:{errors}}=useForm({values:initialData||{}}); const submit=(data)=>{onSubmit(data); reset();}; return <AnimatePresence>{open&&<Dialog static open={open} onClose={onClose} className="relative z-50"><motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 bg-slate-950/60"/><div className="fixed inset-0 grid place-items-center p-4"><Dialog.Panel as={motion.div} initial={{opacity:0,y:30,scale:.96}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:30,scale:.96}} className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-[2rem] bg-white p-6 shadow-2xl dark:bg-slate-900"><Dialog.Title className="text-xl font-bold text-slate-900 dark:text-white">{title}</Dialog.Title><form onSubmit={handleSubmit(submit)} className="mt-5 grid gap-4 sm:grid-cols-2">{columns.filter(c=>c!=='id').slice(0,10).map((c)=><Input key={c} label={c.replaceAll('_',' ')} error={errors[c]?.message} {...register(c,{required:c.includes('title')||c.includes('name')?'Required':false})}/>)}<div className="flex justify-end gap-2 sm:col-span-2"><Button type="button" variant="secondary" onClick={onClose}>Cancel</Button><Button>Save Record</Button></div></form></Dialog.Panel></div></Dialog>}</AnimatePresence> }
